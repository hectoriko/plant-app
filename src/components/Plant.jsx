export default function Plant({plant}) {

    // const { id, name, image, description, price } = guitar;

    return (
        <div className="plant">
            <h2>Plantaa {plant}</h2>
        </div>
    );
}